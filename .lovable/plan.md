

## Plan: Remove Retirement Overlay

Remove the `RetirementOverlay` component from `App.jsx` to restore full site access.

### Changes

1. **`src/App.jsx`** — Remove the `import RetirementOverlay` line and the `<RetirementOverlay />` render call.

That's it — the site will be fully accessible again.

